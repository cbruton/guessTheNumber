use GuessTheNumber;

-- SELECT gameID, inProgress
-- FROM game
-- WHERE gameID = 1;

SELECT * FROM game;

-- INSERT INTO game(gameID, answer, inProgress) VALUES
    
-- INSERT INTO round (roundID, gameID, guessTime, guess, result) VALUES 

    
SELECT * FROM round;

-- select last_insert_id() from game;
